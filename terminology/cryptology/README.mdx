---
hide_title: true
---

import Link from '@docusaurus/Link';

## 基本概念

<ul>
  {[
    '消息, 发送者, 接收者, 窃听者',
    '明文, 密文, 加密, 解密, 算法',
    '密钥, 对称密码, 非对称密码',
    '编码, 比特序列, 编码规则, 进制',
    '哈希函数',
    '消息验证码',
    '数字签名',
    '伪随机数生成器',
    '隐写术',
    '密码与信息安全常识',
    '量子密码',
  ].map(t => (
    <li key={t}>
      <Link to={`./${t}`}>{t}</Link>
    </li>
  ))}
</ul>

## 历史上的密码

<ul>
  {[
    '凯撒密码',
    '穷举搜索',
    '密钥空间',
    '简单替换密码',
    'Enigma',
  ].map(t => (
    <li key={t}>
      <Link to={`./${t}`}>{t}</Link>
    </li>
  ))}
</ul>

## 对称密码

<ul>
  {[
    '一次性密码本',
    'DES',
  ].map(t => (
    <li key={t}>
      <Link to={`./${t}`}>{t}</Link>
    </li>
  ))}
</ul>


