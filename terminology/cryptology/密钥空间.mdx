import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

:::info[Definition]

密钥空间（Key Space）指的是某一加密算法下，所有可能密钥的集合，即可以被选择和使用的全部密钥的总数。

对于一个密码系统，密钥空间越大，安全性通常越高。因为攻击者想用暴力破解（穷举搜索）方式破解加密，需要尝试密钥空间中所有可能密钥，空间越大，攻击者的计算量和时间成本就越高。

:::

