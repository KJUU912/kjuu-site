import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

:::info[Definition]

暴力破解（Brute Force Attack）或穷举搜索（Exhaustive Search）是一种最直接的密码攻击方法，通过尝试所有可能的密钥、密码或组合，直到找到正确答案为止。

攻击者不需要了解算法内部细节，仅依赖“把所有可能情况一个一个试过去”，例如尝试全部可能的密码组合或密钥。

只要密钥空间有限，理论上穷举最终必定能破解加密，但所需计算资源和时间随密钥长度大幅增加。

:::

